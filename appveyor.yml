environment:

  matrix:
    - PYTHON: "C:\\Python27"
    - PYTHON: "C:\\Python34"
    - PYTHON: "C:\\Python35"
    - PYTHON: "C:\\Python27-x64"
    - PYTHON: "C:\\Python34-x64"
      DISTUTILS_USE_SDK: "1"
    - PYTHON: "C:\\Python35-x64"

init:
  - "ECHO %PYTHON%"

install:
  - choco install -y swig
  - "%PYTHON%\\python.exe -m pip install -U pip wheel"

  # Prepend newly installed Python to the PATH of this build (this cannot be
  # done from inside the powershell script as it would require to restart
  # the parent CMD process).
  #- "SET PATH=%PYTHON%;%PYTHON%\\Scripts;%PATH%"

  - "%PYTHON%\\python.exe -m pip install nose twine nose"
  - "appveyor\\build.cmd %PYTHON%/python appveyor/build_glpk.py"

build: off

test_script:
  - "appveyor\\build.cmd %PYTHON%/python -m nose -v"

after_test:
  - "appveyor\\build.cmd %PYTHON%/python setup.py bdist_wheel"

artifacts:
  - path: dist\*

deploy_script:
  - ps: >-
      if($env:appveyor_repo_tag -eq 'True') {
          Invoke-Expression "$env:PYTHON/Scripts/twine upload dist/* --username Nikolaus.Sonnenschein --password $env:password"
      }
